# 9. Credentials And Issuer Trust

A ZK proof is only as meaningful as the credential and issuer policy behind it.

Issuer trust registry requirements:

- issuer id
- accepted credential types
- allowed predicates
- jurisdiction or region scope
- key or root identifiers
- revocation root policy
- audit status

Verifier policies should reject unknown issuers by default.
